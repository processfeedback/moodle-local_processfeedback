define(['local_processfeedback/submission/dashboard_report_button', 'local_processfeedback/submission/single_report_buttons', 'local_processfeedback/submission/report_transfer', 'local_processfeedback/utils/logger'], function(__dep0, __dep1, __dep2, __dep3) {
const initProcessFeedbackDashboardButton = __dep0.initProcessFeedbackDashboardButton;
const getSingleWritingProcessReportLinks = __dep1.getSingleWritingProcessReportLinks;
const initSingleWritingProcessReportButtons = __dep1.initSingleWritingProcessReportButtons;
const getProcessFeedbackZipLinks = __dep2.getProcessFeedbackZipLinks;
const markProcessFeedbackSubmissionTables = __dep2.markProcessFeedbackSubmissionTables;
const debugError = __dep3.debugError;
const debugLog = __dep3.debugLog;
// This file is part of Moodle - http://moodle.org/
//
// Moodle is free software: you can redistribute it and/or modify
// it under the terms of the GNU General Public License as published by
// the Free Software Foundation, either version 3 of the License, or
// (at your option) any later version.
//
// Moodle is distributed in the hope that it will be useful,
// but WITHOUT ANY WARRANTY; without even the implied warranty of
// MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
// GNU General Public License for more details.
//
// You should have received a copy of the GNU General Public License
// along with Moodle.  If not, see <http://www.gnu.org/licenses/>.





const initAssignmentReportActions = (state, windowRef, documentRef) => {
    if (!state || !state.params || state.params.moduleName !== 'assign') {
        debugLog(windowRef, 'Assignment report actions skipped: not an assignment page');
        return;
    }

    const links = getProcessFeedbackZipLinks(documentRef);
    const singleReportLinks = getSingleWritingProcessReportLinks(documentRef);
    if (links.length === 0 && singleReportLinks.length === 0) {
        debugLog(windowRef, 'Assignment report actions skipped: no report ZIP links found');
        return;
    }

    debugLog(windowRef, 'Assignment report actions initialising', {
        dashboardLinkCount: links.length,
        singleReportLinkCount: singleReportLinks.length,
    });
    markProcessFeedbackSubmissionTables(links);
    initSingleWritingProcessReportButtons(windowRef, documentRef, state).catch((error) => {
        debugError(windowRef, 'Single writing-process report button initialisation failed', error);
    });
    if (links.length > 0) {
        initProcessFeedbackDashboardButton(windowRef, documentRef).catch((error) => {
            debugError(windowRef, 'Dashboard report button initialisation failed', error);
        });
    }
};

const initAssignmentReportActionsForCurrentPage = () => initAssignmentReportActions({
    params: {
        moduleName: 'assign',
    },
}, window, document);
return { initAssignmentReportActions, initAssignmentReportActionsForCurrentPage };
});